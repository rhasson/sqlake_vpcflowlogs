name: development

on:
  push:
    branches: [main, dev]

jobs:
  run_worksheet:
<<<<<<< HEAD
=======
    environment: development
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v0.1')
>>>>>>> 104bc54ab13bc0caa2f714fb4fe51652d6dde8a0
    name: Run Worksheet
    environment:
      name: development
    if: github.event.ref == 'refs/heads/dev'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run Worksheet
        uses: rhasson/sqlake_action@main
        with:
          worksheet_path: src
          api_key: ${{ secrets.API_KEY }}
      - name: Print Results
        run: |
          cat ${GITHUB_WORKSPACE}/worksheet_output.md >> $GITHUB_STEP_SUMMARY
